# Fallin: 보행 환경과 날씨를 지도 위에서 읽는 고령자 낙상 위험 프로토타입

[English](PORTFOLIO.md) | [한국어](PORTFOLIO.ko.md)

## 한눈에 보기

Fallin은 고령자가 걷는 길에서 경사, 보도 위험요소, 조도, 날씨가 겹치는 구간을 먼저 보여 주자는 아이디어에서 출발했다. 제11회 인천광역시 공공데이터 활용 경진대회 아이디어 기획 제안서에는 위험 지도, 안전 경로, 보호자 알림, 스마트폰 충격 감지까지 포함한 서비스 청사진이 담겨 있다. 저장소에는 그중 **GIS 입력과 날씨를 결합해 구간별 위험 점수를 매기고 Folium 지도에 표시하는 규칙 기반 프로토타입**이 구현돼 있다.

제안서 역할표에는 이준형의 담당 업무가 ‘CTO / S/W 개발 총괄’로 기록돼 있다. 실제 코드 범위는 Shapefile 로딩·좌표계 변환, 기상청 API 또는 데모용 날씨, 위험 점수 계산, HTML 지도 생성까지다. 앱, 보호자 푸시, 충격 감지, 안전 경로 추천은 제안된 서비스 범위이지 이 저장소에서 완성·검증된 기능은 아니다.

## 왜 ‘사고 예측’보다 ‘위험 구간 가시화’부터 시작했나

제안서는 기존 낙상 서비스가 사고 뒤 감지에 머물고, 실외 보행 환경의 위험을 미리 알려 주는 수단이 부족하다는 문제를 짚었다. 낙상에는 개인 건강·약물·보행 습관 같은 요인도 크지만, 이 프로토타입은 개인 의료 데이터를 다루지 않고 공공 데이터로 관찰 가능한 **외부 보행 환경**에 범위를 한정했다. 그래서 산출물도 개인별 임상 위험도가 아니라 보행 구간의 상대적 위험 점수다.

초기 판단 근거는 다음 네 축이다.

| 축 | 코드에서 사용하는 예시 | 의도 |
|---|---|---|
| 지형 | 평균 경사도 | 오르막·내리막에서의 보행 부담 반영 |
| 보행 환경 | 위험요소 개수, 조도 | 보도 상태와 시야 조건 반영 |
| 날씨 | 기온, 습도, 강수, 적설, 풍속 | 미끄러움·결빙 가능성이 높아지는 상황 반영 |
| 공간 표현 | 구간 geometry와 중심점 | 결과를 지도에서 빠르게 확인 |

이 분리는 중요하다. 제안서에는 향후 의료기관 낙상 신고, 스마트폰 센서, 사용자 행동 데이터를 붙이는 계획이 있지만, 현재 결과가 그런 개인 요인까지 학습한 AI 예측 모델이라는 뜻은 아니다.

## 구현한 파이프라인

`main.py`는 한 번의 명령으로 네 단계를 실행한다.

```text
Shapefile 입력 → GeoPandas 로딩·EPSG:4326 변환 → 날씨 수집 → 구간별 규칙 점수 → Folium HTML 지도
```

1. `GISDataLoader`는 입력 Shapefile을 읽고, 지도가 사용할 WGS84(EPSG:4326) 좌표계로 변환한다. CRS가 없으면 오류를 내도록 해 서로 다른 좌표계가 무심코 섞이는 일을 막았다.
2. `WeatherFetcher`는 `KMA_SERVICE_KEY`가 있을 때 기상청 초단기예보 API에서 `TMP`, `REH`, `PTY`, `PCP`, `SNO`, `WSD` 값을 받는다. 키가 없거나 요청이 실패하면 겨울·강수 상황을 고정한 데모값을 반환한다.
3. `RiskCalculator`는 각 보행 구간에 점수를 더한다. 평균 경사도가 7도를 넘으면 +5점, 5도를 넘으면 +3점이다. 위험요소 수가 3개 이상이면 +5점, 1개 이상이면 +3점이다. 강수·강설, 결빙에 가까운 기온, 높은 습도, 강풍에도 가산점이 붙는다.
4. 점수는 `Low`(0~5), `Medium`(6~10), `High`(11~20)로 나눈다. `MapVisualizer`는 전체 구간을 heatmap으로 표시하고, 고위험 구간의 중심점에는 빨간 marker를 올린 HTML 파일을 만든다.

기상 API 없이도 화면 흐름을 확인할 수 있도록 데모값을 넣은 것은 프로토타입에는 유용하다. 다만 이 경우 생성된 지도는 당시 실제 날씨를 반영하지 않는다. 결과를 볼 때 API 사용 여부를 함께 확인해야 하는 이유다.

## 제안서에서 검토한 데이터와 서비스 확장

제안서에는 기상청 동네예보, 인천시 이동약자 관련 데이터, 도로 조도·온도·습도·유동인구 데이터, 추락·낙상사고 API, KOSIS 통계를 후보로 적었다. 구현 파일에는 `mean_slope_degree`, `risk_count`, 조도 컬럼과 날씨 필드를 읽는 로직이 남아 있다. 보고서에는 인주대로 일대의 위험 지도 예시도 제시돼 있으며, 현장에서 노란색으로 보인 구간에 횡단보도 턱과 육교 계단이 있었다는 관찰을 기록했다.

서비스 수준의 제안은 더 넓다. 반경 500m 위험 지도, 고령자 친화 UI, 보호자 알림, 우회 경로, 예방 콘텐츠와 커머스까지 이어지는 B2C/B2G 모델을 구상했다. 이 내용은 문제를 사업과 정책으로 연결한 기획 성과로 소개할 수 있다. 다만 월 구독료, 사용자 수, 낙상률 감소, 정확도 목표 등 보고서의 숫자는 사업 계획 또는 목표치이며 현재의 측정 성과가 아니다.

## 결과를 정확히 읽는 기준

실행 결과는 제공된 공간 데이터의 각 구간에 점수와 위험 등급을 붙인 HTML 지도다. 저장소에 완전한 Shapefile sidecar와 출처 메타데이터, 실제 낙상 발생 라벨, 현장 검증 로그가 남아 있지 않다. 따라서 “낙상을 85% 예측했다”거나 “안전 경로가 사고를 줄였다”는 표현은 쓸 수 없다. 코드의 가중치는 관찰된 사고 데이터로 보정한 계수가 아니라, 위험요소를 빠르게 조합해 보기 위한 명시적 설계 규칙이다.

그렇다고 단순한 아이디어 문서에 그친 것은 아니다. 공간 데이터를 읽어 지도 좌표계로 바꾸고, 외부 기상 값에 따라 같은 도로 구간의 점수가 달라지도록 연결했으며, 사용자가 이해할 수 있는 지도 산출물까지 만든 프로토타입이다. 다음 단계에서 실제 사고·민원·보행 데이터와 결합해 규칙의 가중치를 검증하면, 이 초기 모델을 지자체 정비 우선순위나 사용자 경고 기준으로 발전시킬 수 있다.

## 다음 단계

- 허용 가능한 범위에서 완전한 Shapefile 구성 파일과 데이터 출처·수집일을 보관한다.
- 실제 낙상·보행 안전 사건을 공간적으로 연결하고, 시간·공간 분리 검증으로 규칙을 보정한다.
- API 호출 시점·좌표·응답을 기록해 지도 결과를 재현 가능하게 만든다.
- 서비스 기획 기능(안전 경로·보호자 알림·센서 감지)은 별도 앱 모듈과 사용자 테스트로 구현 여부를 검증한다.

## 근거 파일

- 공모전 제안서: [`reports/fallin_incheon_open_data.pdf`](reports/fallin_incheon_open_data.pdf)
- 실행 진입점: [`main.py`](main.py)
- 위험 규칙: [`src/risk_calculator.py`](src/risk_calculator.py)
- GIS·날씨·지도 모듈: [`src/data_loader.py`](src/data_loader.py), [`src/weather.py`](src/weather.py), [`src/map_visualizer.py`](src/map_visualizer.py)
